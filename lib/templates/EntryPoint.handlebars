import { AppRegistry } from 'react-native';
import { App } from './src';
import StorybookUI from './storybook';

var storybook = true;

storybook ? 
AppRegistry.registerComponent('{{projectName}}', () => StorybookUI) :
AppRegistry.registerComponent('{{projectName}}', () => App);